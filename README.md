# BSH-DBMS
Bash Shell Script Database Management System (DBMS): The Project aims to develop DBMS that will enable users to store and retrieve the data from Hard-disk.

## Installation

To set up the project, run the following command in your terminal:

```bash
chmod +x setup.sh && ./setup.sh
```